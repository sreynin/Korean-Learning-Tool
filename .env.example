# --- Core -------------------------------------------------------------------
# Public base URL of the app. Used for absolute links and, later, OAuth redirects.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Directory where the local JSON data store is written.
# Relative paths resolve from the project root.
DATA_DIR=./data

# Seed the store with example projects the first time it is created.
SEED_SAMPLE_DATA=true

# --- AI lesson generation ---------------------------------------------------
# Anthropic API key. Leave blank to use the built-in mock generator, which
# returns clearly-labelled sample lessons so the flow works without credentials.
AI_API_KEY=

# Model used for lesson generation.
AI_MODEL=claude-opus-5

# --- Not wired up yet -------------------------------------------------------
# These are declared so deployment configuration can be prepared in advance.
# Leave them blank until the matching feature is implemented.

# Text-to-speech narration
ELEVENLABS_API_KEY=

# YouTube upload + metadata
YOUTUBE_CLIENT_ID=
YOUTUBE_CLIENT_SECRET=
